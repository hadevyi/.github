---
name: PR 담당자 자동 지정
'on':
  pull_request_target:
    types:
    - opened
    - reopened
    - ready_for_review
permissions:
  contents: read
  pull-requests: write
jobs:
  assign:
    uses: "./.github/workflows/reusable-pull-request-auto-assign.yml"
